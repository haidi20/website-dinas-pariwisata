import React, {Component} from 'react';

export default function SlideImage (props) {
    // setTimeout(() => {
        
    // }, 0);
    return(
        <img src={`images/pemerintah/POSTER BARU PARIWISATA-0${props.number}.jpg`} alt="" />
    )
}